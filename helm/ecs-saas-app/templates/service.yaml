apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecs-saas-app.fullname" . }}
  labels:
    {{- include "ecs-saas-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ecs-saas-app.selectorLabels" . | nindent 4 }}
